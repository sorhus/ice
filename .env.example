# Copernicus Data Space credentials
# Register at: https://dataspace.copernicus.eu/
COPERNICUS_USER=your_username
COPERNICUS_PASSWORD=your_password

# Optional: Run collection on container startup (default: true)
# Set to false to only run via cron schedule
RUN_ON_STARTUP=true
